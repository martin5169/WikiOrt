@model IEnumerable<WikiORT.Models.Mensaje>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Texto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Articulo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Texto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Articulo.ArticuloId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MensajeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MensajeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MensajeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
